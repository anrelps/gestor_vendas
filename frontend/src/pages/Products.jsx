import ProductsList from '../components/ProductsList';
import Layout from '../Layout';

const Products = () => {
  return (
    <Layout>
      <ProductsList />
    </Layout>
  );
};

export default Products;
