import ClientList from '../components/ClientList';
import Layout from '../Layout';

const Clients = () => {
    return (
        <Layout>
            <ClientList />
        </Layout>
    );
};

export default Clients;
